/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../../mock-server/MockServerPool";
import { phenomlClient } from "../../../src/Client";

describe("Authtoken", () => {
    test("generateToken", async () => {
        const server = mockServerPool.createServer();
        const client = new phenomlClient({ token: "test", environment: server.baseUrl });

        const rawResponseBody = { token: "token" };
        server.mockEndpoint().post("/auth/token").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.authtoken.generateToken();
        expect(response).toEqual({
            token: "token",
        });
    });
});
