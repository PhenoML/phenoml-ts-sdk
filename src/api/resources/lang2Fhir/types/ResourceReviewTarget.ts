// This file was auto-generated by Fern from our API Definition.

export interface ResourceReviewTarget {
    /** FHIR resourceType to audit (e.g. Condition, Procedure, Observation, Encounter, MedicationRequest), matched against each generated resource's resourceType. Patient is not supported. */
    resource_type: string;
    /** Field kinds to check for this resource type. Defaults to ["dates"] when omitted; use "codes" to audit clinical code concepts. */
    fields?: ResourceReviewTarget.Fields.Item[] | undefined;
}

export namespace ResourceReviewTarget {
    export type Fields = Fields.Item[];

    export namespace Fields {
        export const Item = {
            Dates: "dates",
            Codes: "codes",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
