// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as PhenoML from "../../../index.js";

export declare namespace CohortClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class CohortClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<CohortClient.Options>;

    constructor(options: CohortClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Converts natural language text into structured FHIR search queries for patient cohort analysis
     *
     * @param {PhenoML.cohort.CohortRequest} request
     * @param {CohortClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link PhenoML.cohort.BadRequestError}
     * @throws {@link PhenoML.cohort.UnauthorizedError}
     * @throws {@link PhenoML.cohort.InternalServerError}
     *
     * @example
     *     await client.cohort.analyze({
     *         text: "female patients over 65 with diabetes but not hypertension"
     *     })
     */
    public analyze(
        request: PhenoML.cohort.CohortRequest,
        requestOptions?: CohortClient.RequestOptions,
    ): core.HttpResponsePromise<PhenoML.cohort.CohortResponse> {
        return core.HttpResponsePromise.fromPromise(this.__analyze(request, requestOptions));
    }

    private async __analyze(
        request: PhenoML.cohort.CohortRequest,
        requestOptions?: CohortClient.RequestOptions,
    ): Promise<core.WithRawResponse<PhenoML.cohort.CohortResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PhenoMLEnvironment.Default,
                "cohort",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as PhenoML.cohort.CohortResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new PhenoML.cohort.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 401:
                    throw new PhenoML.cohort.UnauthorizedError(_response.error.body as unknown, _response.rawResponse);
                case 500:
                    throw new PhenoML.cohort.InternalServerError(
                        _response.error.body as unknown,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PhenoMLError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/cohort");
    }
}
