// This file was auto-generated by Fern from our API Definition.

import type * as phenoml from "../../../index.js";

/**
 * Predefined role that maps to a set of OAuth scopes. Cannot be specified with `scopes`.
 * The role is validated against the provider's allowed roles and used to derive scopes at creation
 * time. Only the resulting scopes are stored and returned in responses. Only `client_secret`, `jwt`,
 * and `on_behalf_of` auth methods use these scopes for token acquisition.
 * If neither `role` nor `scopes` is specified, the provider-specific default role is used.
 *
 * Valid roles depend on the provider type — see the per-provider schemas below.
 * Canvas, Google Healthcare, and HAPI do not support roles. Canvas only supports user-level
 * scoping; when using `client_secret` or `jwt` auth with Canvas, you must specify `scopes`
 * explicitly instead.
 */
export type Role =
    | phenoml.fhirProvider.EpicRole
    | phenoml.fhirProvider.CernerRole
    | phenoml.fhirProvider.AthenaHealthRole
    | phenoml.fhirProvider.MedplumRole
    | phenoml.fhirProvider.ElationRole
    | phenoml.fhirProvider.PhenostoreRole;
